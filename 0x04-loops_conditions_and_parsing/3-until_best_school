#!/usr/bin/env bash
# This script displays "Best School" 10 times using an until loop

# Initialize a counter variable
counter=0

# Use an until loop to iterate until the counter reaches 10
until [ $counter -ge 10 ]
do
    # Display "Best School" in each iteration
    echo "Best School"

    # Increment the counter
    ((counter++))
done
